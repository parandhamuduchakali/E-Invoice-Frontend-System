# Base URL of the E-Invoice backend.
# Leave empty in development: the Vite dev server proxies /api to http://127.0.0.1:8000.
# For a production build point it at the deployed API, e.g. https://api.example.com
VITE_API_BASE_URL=
